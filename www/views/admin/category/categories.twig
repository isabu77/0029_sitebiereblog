{% extends "/layout/admindefault.twig" %}

{% block content %}
<section class="admin-categories">
    <h2>Liste des categories</h2>
    <p><a href="{{ uri("admin_category_insert") }}">Ajouter une categorie</a></p>
{% for category in categories %}
    <article class="onePost">
    <h3>{{ category.name }}</h3>
    <p>SLUG : {{ category.slug }}</p>
    <a href="{{ category.AdminUrl }}">Editer</a>
    </article>
{% endfor %}
</section>
{{ paginate | raw }}
{% endblock %}