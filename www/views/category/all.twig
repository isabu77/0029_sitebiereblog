{% extends "layout/default.twig" %}

{% block content %}
<ul>
    {% for category in categories %}
    <li><a href="{{ category.url }}">{{ category.name }}</a></li>
    {% endfor %}
</ul>
{{ paginate | raw }}
{% endblock %}